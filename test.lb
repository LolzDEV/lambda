# vector definition
s :: () -> vector[2]:= [1, 3]

# matrix definition
m :: () -> matrix[4,4]:= [
    1, 2, 3, 4
    3, 4, 5, 6
    1, 3, 2, 5
    3, 6, 4, 6
]

# constant definition
k :: () -> number := λ.3

# function definition 
x :: number -> number -> number := λa.λb.a+b

# applying function
k1 := λ.(1 2 x)

y := λx.(let i = m[x]; i y)
